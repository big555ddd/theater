<style>
    article, aside, figure, footer, header, hgroup, 
    menu, nav, section { display: block; }
    #modal {
      width: 400px;
      border: 1px solid gray;
      box-shadow: 0 1px 5px gray;
      border-radius: 5px;
      font-family: verdana;
      margin: 25px auto;
      overflow: hidden;
      background-color: #f1f1f1;
    }
    #modal header {
      background: #f1f1f1;
      background-image: -webkit-linear-gradient( top, #f1f1f1, gray );
      background-image: -ms-linear-gradient( top, #f1f1f1, gray );
      background-image: -moz-linear-gradient( top, #f1f1f1, gray );
      background-image: -o-linear-gradient( top, #f1f1f1, gray );
      box-shadow: 0 1px 2px #888;
      padding: 10px;
    }
    #modal h1 {
      padding: 0;
      margin: 0;
      font-size: 16px;
      font-weight: normal;
      text-shadow: 0 1px 2px white;
      color: #f1f1f1;
      text-align: center;
    }
    #modal section {
      padding: 10px 30px; 
      font-size: 12px;
      line-height: 175%;
      color: #f1f1f1;
    }
    .topright {
  position: absolute;
  text-align: center;
  top: 150px;
  right: 750px;
  font-size: 18px;
}
  </style>
  </head>
  <body>
  
    <div id="modal" class="topright">
      <header><h1></h1></header>
      <section>
        
        <p>First name:<%= user.firstName%></p>
       <p>Last name:<%= user.lastName %></p>
        <p>Username:<%= currentUser.username %></p>
        <p>Email:<%= user.email %></p>
      </section>
    </div>
  
  </body>
  </html>